PHASE 4 - CALCULATOR APP

📌 Description:
A simple calculator app built using HTML, CSS (Grid layout), and beginner-friendly JavaScript. This app supports basic operations: +, −, ×, ÷, clear, delete, and evaluate.

💻 Features:
- Responsive grid-based UI
- Displays live input and result
- Supports clear (AC), backspace (Del), decimal, and basic operators
- Styled to match a dark theme modern calculator

🧱 Tech Stack:
- HTML: Structure of the calculator
- CSS: Grid layout + dark aesthetic design
- JavaScript: Input handling, evaluation logic

🧠 JavaScript Logic:
- Buttons selected with `querySelectorAll`
- Display updated on every button click
- `AC` → clears input  
- `Del` → removes last character  
- `=` → evaluates expression using `eval()`
- All others append to the string and update display

🧪 Example Flow:
Click: `7` → `+` → `3` → `=`  
Output: `10`

📂 Files Used:
- index.html
- style.css
- app.js

✅ Completed:
✔ Grid layout
✔ Button functionality
✔ Display result
✔ Delete & AC buttons
✔ Enter expression via button clicks only

Next Phase ➡ Weather App (API & Dynamic DOM coming up!)

